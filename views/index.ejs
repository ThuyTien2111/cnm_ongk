<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="index.css">
</head>
<body>
    <div class="content">
        <h1>Form sản phẩm</h1>
        <form action="/add" method="post" enctype="multipart/form-data">
            <input type="text" name="id" placeholder="Ma san pham">
            <br>
            <input type="text" name="name" placeholder="Ten san pham">
            <br>
            <input type="text" name="category" placeholder="Loai san pham">
            <br>
            <input type="file" name="image" accept="image/">
            <br>
            <button type="submit">Thêm sản phẩm</button>
        </form>
        <br>
        <form action="/delete" method="post">
            <h2>Danh sach san pham</h2>
            <table>
                <thead>
                    <tr>
                        <th>STT</th>
                        <th>Ten san pham</th>
                        <th>Loai san pham</th>
                        <th>Anh san pham</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(i=0; i<data.length; i++){ %>
                        <tr>
                            <td><%= data[i].id %></td>
                            <td><%= data[i].name %></td>
                            <td><%= data[i].category %></td>
                            <td>
                                <!-- phai co px -->
                                <img src="<%= data[i].image %>" alt="<%= data[i].id %>" style="max-width: 100px; max-height: 100px" >
                            </td>
                            <td>
                                <input type="checkbox" name="selectedProduct" value="<%= data[i].id %>" >
                            </td>
                        </tr>
                    <% }%>
                </tbody>
            </table>
            <button type="submit">Xoá sản phẩm</button>
        </form>
    </div>
    
</body>
</html>